repeat wait() until game:IsLoaded()

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local VirtualUser = game:GetService("VirtualUser")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

-- 🔄 Chặn AFK
LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

-- 🏴 Tự động chọn Hải Tặc khi vào game
if game.PlaceId == 2753915549 or game.PlaceId == 4442272183 or game.PlaceId == 7449423635 then
    local args = { [1] = "SetTeam", [2] = "Pirates" }
    ReplicatedStorage.Remotes.CommF_:InvokeServer(unpack(args))
    wait(1)
    ReplicatedStorage.Remotes.CommF_:InvokeServer("TeleportToSpawn")
end

-- 📌 Tạo UI hiển thị trên màn hình
local function CreateUI()
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "PlowHubUI"
    screenGui.Parent = game.Players.LocalPlayer:FindFirstChild("PlayerGui") or game.CoreGui

    local Frame = Instance.new("Frame", screenGui)
    Frame.Size = UDim2.new(0, 320, 0, 200)
    Frame.Position = UDim2.new(0.5, -160, 0.05, 0)
    Frame.BackgroundColor3 = Color3.new(0, 0, 0)
    Frame.BackgroundTransparency = 0.3
    Frame.BorderSizePixel = 2

    -- 🌐 Tiêu đề UI
    local Title = Instance.new("TextLabel", Frame)
    Title.Size = UDim2.new(1, 0, 0.2, 0)
    Title.TextScaled = true
    Title.TextColor3 = Color3.fromRGB(173, 216, 230)
    Title.BackgroundTransparency = 1
    Title.Text = "🌐 Plow Game | Community"

    -- 🔍 Khung Trạng thái trái ác quỷ
    local StatusFrame = Instance.new("Frame", Frame)
    StatusFrame.Size = UDim2.new(0.9, 0, 0.4, 0)
    StatusFrame.Position = UDim2.new(0.05, 0, 0.3, 0)
    StatusFrame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    StatusFrame.BackgroundTransparency = 0.2

    local StatusLabel = Instance.new("TextLabel", StatusFrame)
    StatusLabel.Size = UDim2.new(1, 0, 1, 0)
    StatusLabel.TextSize = 18
    StatusLabel.TextColor3 = Color3.new(1, 1, 1)
    StatusLabel.BackgroundTransparency = 1
    StatusLabel.Text = "🔎 Đang kiểm tra trái ác quỷ..."

    -- 🎯 Đếm ngược thời gian
    local CountdownLabel = Instance.new("TextLabel", Frame)
    CountdownLabel.Size = UDim2.new(1, 0, 0.15, 0)
    CountdownLabel.Position = UDim2.new(0, 0, 0.75, 0)
    CountdownLabel.TextSize = 16
    CountdownLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
    CountdownLabel.BackgroundTransparency = 1
    CountdownLabel.Text = "⏳ Chuyển server trong: 5 giây"

    return StatusLabel, CountdownLabel
end

-- 📌 Khởi tạo UI
local StatusLabel, CountdownLabel = CreateUI()

-- 🔍 Tìm trái ác quỷ nhanh hơn
local function FindFruits()
    for _, fruit in pairs(game.Workspace:GetChildren()) do
        if fruit:IsA("Tool") and string.find(fruit.Name, "Fruit") then
            return fruit
        end
    end
    return nil
end

-- 📍 Dịch chuyển đến trái
local function TeleportToPosition(position)
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        LocalPlayer.Character.HumanoidRootPart.CFrame = position
    end
end

-- ✅ Lưu trữ trái vào kho
local function StoreFruit(fruit)
    -- Kiểm tra xem nhân vật có cầm trái ác quỷ không
    if fruit then
        -- Lưu trữ trái vào kho
        local fruitName = fruit.Name
        ReplicatedStorage.Remotes.CommF_:InvokeServer("StoreFruit", fruitName)

        -- Cập nhật trạng thái UI sau khi lưu trái
        StatusLabel.Text = "✅ Đã lưu trữ trái " .. fruitName .. " vào kho!"
    else
        -- Nếu không có trái, cập nhật UI
        StatusLabel.Text = "❌ Không có trái để lưu!"
    end
end

-- 🌐 **Tự động Server Hop với bộ đếm 5 giây**
local function ServerHopWithCountdown()
    for i = 5, 1, -1 do
        CountdownLabel.Text = "⏳ Chuyển server trong: " .. i .. " giây"
        wait(1)
    end
    CountdownLabel.Text = "🔁 Đang chuyển server..."
    
    local servers = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/2753915549/servers/Public?sortOrder=Asc&limit=100")).data
    for _, server in ipairs(servers) do
        if server.playing < server.maxPlayers then
            TeleportService:TeleportToPlaceInstance(game.PlaceId, server.id, LocalPlayer)
            return
        end
    end

    StatusLabel.Text = "❌ Không tìm thấy server phù hợp!"
end

-- 🔁 **Tìm & nhặt trái, sau đó tự động Server Hop với đếm ngược**
while true do
    local fruit = FindFruits()
    if fruit then
        StatusLabel.Text = "🍏 Đã tìm thấy trái ác quỷ!"
        TeleportToPosition(fruit.Handle.CFrame)
        wait(1)

        -- 🏆 Nhặt trái và lưu vào kho
        LocalPlayer.Character.Humanoid:EquipTool(fruit)  -- Nhặt trái
        wait(1)
        StoreFruit(fruit)  -- Lưu vào kho
    else
        StatusLabel.Text = "❌ Không có trái! Chuẩn bị chuyển server..."
        ServerHopWithCountdown()
    end

    wait(5) -- ⏳ Chờ 5 giây trước khi kiểm tra tiếp
end
