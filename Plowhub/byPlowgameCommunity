-- Khai báo các service cần thiết
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local VirtualUser = game:GetService("VirtualUser")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

-- Chặn AFK
LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

-- Tự động chọn Hải Tặc khi vào game
if game.PlaceId == 2753915549 or game.PlaceId == 4442272183 or game.PlaceId == 7449423635 then
    local args = { [1] = "SetTeam", [2] = "Pirates" }
    ReplicatedStorage.Remotes.CommF_:InvokeServer(unpack(args))
    wait(1)
    ReplicatedStorage.Remotes.CommF_:InvokeServer("TeleportToSpawn")
end

-- Hàm để chuyển đến server khác (không phải server hop)
local function SwitchServer()
    -- Đặt lại PlaceId nếu cần (thông thường là PlaceId của game bạn muốn chuyển đến)
    local placeId = game.PlaceId

    -- Chuyển đến server khác sử dụng TeleportService (không dùng Server Hop)
    -- Có thể đổi server bất kỳ mà không cần phải lọc server ít người chơi.
    TeleportService:Teleport(placeId, LocalPlayer)
    print("Đang chuyển đến server mới...")
end

-- Hàm hiển thị UI thông báo
local function CreateUI()
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "PlowHubUI"
    screenGui.Parent = game.Players.LocalPlayer:FindFirstChild("PlayerGui") or game.CoreGui

    local Frame = Instance.new("Frame", screenGui)
    Frame.Size = UDim2.new(0, 320, 0, 200)
    Frame.Position = UDim2.new(0.5, -160, 0.05, 0)
    Frame.BackgroundColor3 = Color3.new(0, 0, 0)
    Frame.BackgroundTransparency = 0.3
    Frame.BorderSizePixel = 2
    Frame.BorderRadius = UDim.new(0, 16)  -- Bo tròn góc

    local Title = Instance.new("TextLabel", Frame)
    Title.Size = UDim2.new(1, 0, 0.2, 0)
    Title.Text = "🌐 Plow Game | Community"
    Title.TextScaled = true
    Title.TextColor3 = Color3.new(1, 1, 1)
    Title.BackgroundTransparency = 1

    return Frame
end

-- Khởi tạo UI
local frame = CreateUI()

-- Hàm tìm trái ác quỷ
local function FindFruit()
    for _, fruit in pairs(game.Workspace:GetChildren()) do
        if fruit:IsA("Tool") and string.find(fruit.Name, "Fruit") then
            return fruit
        end
    end
    return nil
end

-- Hàm lưu trữ trái vào kho
local function StoreFruit(fruit)
    local success, message = pcall(function()
        -- Gọi lệnh lưu trữ vào kho
        ReplicatedStorage.Remotes.CommF_:InvokeServer("StoreFruit", fruit.Name)
    end)

    -- Kiểm tra nếu lưu thành công
    if success then
        print("Trái " .. fruit.Name .. " đã được lưu trữ vào kho!")
        return true
    else
        print("Lỗi khi lưu trữ trái: " .. message)
        return false
    end
end

-- Hàm kiểm tra trái đã được lưu trữ vào kho chưa (kiểm tra rương)
local function CheckFruitInStorage(fruitName)
    local success, storedFruits = pcall(function()
        -- Giả sử chúng ta có thể truy xuất danh sách trái đã lưu từ một server hoặc dịch vụ
        return ReplicatedStorage.Remotes.CommF_:InvokeServer("GetStoredFruits")
    end)

    if success then
        -- Kiểm tra xem trái đã được lưu chưa
        for _, storedFruit in pairs(storedFruits) do
            if storedFruit == fruitName then
                return true
            end
        end
    end
    return false
end

-- Hàm để bắt đầu quy trình chuyển server và kiểm tra trái ác quỷ
local function StartServerSwitch()
    while true do
        -- Kiểm tra trái ác quỷ có sẵn không
        local fruit = FindFruit()
        if fruit then
            -- Nếu tìm thấy trái, nhặt và lưu trữ
            LocalPlayer.Character.Humanoid:EquipTool(fruit)

            -- Lưu trái vào kho và kiểm tra lưu thành công
            local stored = StoreFruit(fruit)
            if stored then
                -- Kiểm tra nếu trái đã được lưu vào kho thành công
                local success = CheckFruitInStorage(fruit.Name)
                if success then
                    print("Trái " .. fruit.Name .. " đã được lưu vào kho thành công!")
                else
                    print("Trái " .. fruit.Name .. " không thể lưu trữ vào kho!")
                end
            end
        else
            -- Nếu không có trái, thực hiện chuyển server
            SwitchServer()
        end
        wait(5) -- Chờ 5 giây trước khi kiểm tra lại
    end
end

-- Bắt đầu quy trình chuyển server
StartServerSwitch()
